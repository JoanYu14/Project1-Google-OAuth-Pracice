<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>Login</title>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <main style="padding: 3rem">
      <div class="row">
        <div ass="col-md-12">
          <!-- 這個a標籤會是一個按鈕的樣子，按下按鈕就是對localhost:8080/auth/google寄出一個GET Request -->
          <a
            class="btn btn-lg btn-google"
            style="
              padding: 0.2rem 0.5rem;
              background-color: rgb(7, 7, 7);
              color: rgb(255, 255, 255);
            "
            href="/auth/google"
            ><img
              src="https://img.icons8.com/color/16/000000/google-logo.png"
            />
            透過Google登入</a
          >
        </div>
      </div>
      <hr />
      <h5>已經註冊過一個帳戶？您可以在此登錄：</h5>
      <!-- 送出這個form就是對localhost:8080/auth/login寄出一個POST Request -->
      <form action="/auth/login" method="POST">
        <!-- 這個嵌入的ejs檔，如果是直接進入這個頁面是不會有任何element的，要有經過route處理過重新導向這個頁面的才會有element -->
        <!-- 因為這個message.ejs裡面有判斷幾個flash是不是空字串，是的話就不會有任何element，而幾個flash的key所對應的值是在其他route裡才會賦予的 -->
        <%- include ("partials/message") %>
        <div class="form-group">
          <label for="exampleInputEmail1">信箱地址：</label>
          <input
            type="email"
            class="form-control"
            id="exampleInputEmail1"
            aria-describedby="emailHelp"
            name="username"
          />
          <small id="emailHelp" class="form-text text-muted"
            >我們不會與其他人分享你的信箱。</small
          >
        </div>
        <br />
        <div class="form-group">
          <label for="exampleInputPassword1">密碼：</label>
          <input
            type="password"
            class="form-control"
            id="exampleInputPassword1"
            name="password"
          />
        </div>
        <br />
        <button class="btn btn-primary">會員登入</button>
      </form>
    </main>
  </body>
</html>
